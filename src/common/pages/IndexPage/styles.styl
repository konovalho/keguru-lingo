.root
  text-align center
  font-family: Pfencore, Helvetica, sans-serif;
 
.title
  font-size 50px
  font-weight 700
  margin-bottom 10px

.logo
  margin-bottom 10px

.desc
  font-size 20px
  margin-bottom 40px

.itemWord
  display flex

.word
  font-weight 700

.list
  margin 0 auto
  margin-top 30px
  display flex
  flex-direction column
  align-items center